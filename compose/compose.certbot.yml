services:
  certbot:
    image: certbot/certbot:latest
    container_name: vpnforge-certbot
    networks:
      - vpnforge
    volumes:
      - ${VPNFORGE_RUNTIME_DIR:-/var/lib/vpnforge}/certbot/www:/var/www/certbot
      - ${VPNFORGE_RUNTIME_DIR:-/var/lib/vpnforge}/certbot/conf:/etc/letsencrypt
